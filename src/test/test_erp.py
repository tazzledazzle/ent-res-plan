import unittest


class TestERP(unittest.TestCase):
    def test_erp(self):
        self.assertTrue(False)
        # erp_file = "path/to/erp_file.erp"
        # data = parse_erp(erp_file)
        # self.assertEqual(len(data), 100)